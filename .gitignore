# Dependencies
node_modules/

# Environment files — NEVER commit these
.env
.env.local
.env.production

# Build outputs
dist/
.next/
out/

# Uploads (local dev only — use cloud storage in prod)
backend/uploads/

# OS / Editor
.DS_Store
.vscode/
*.log

# Prisma generated (regenerated on deploy)
backend/node_modules/.prisma/
