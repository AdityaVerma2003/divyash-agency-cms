NODE_ENV=development
PORT=4000

DATABASE_URL="postgresql://divyash:divyash_dev_password@localhost:5432/divyash_agency?schema=public"

JWT_ACCESS_SECRET=change_this_access_secret
JWT_REFRESH_SECRET=change_this_refresh_secret
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

CLIENT_ORIGIN=http://localhost:3000

# Filled in during the "billing core" phase
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=

# Email (used by: password reset, payment confirmations, invoice reminders)
# Leave blank to use console-log fallback in development
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="Divyash Digital <billing@divyashdigital.co.in>"
